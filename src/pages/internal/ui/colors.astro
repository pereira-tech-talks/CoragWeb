---
import ShowcaseLayout from '@/layouts/ShowcaseLayout.astro';

interface Token {
  name: string;
  cssVar: string;
  bgClass: string;
  textOnClass: string;
  lightHex: string;
  darkHex: string;
  usage: string;
}

const ptTokens: Token[] = [
  {
    name: 'Primary',
    cssVar: '--color-corag-primary',
    bgClass: 'bg-corag-primary',
    textOnClass: 'text-white',
    lightHex: '#1F6F73',
    darkHex: '#3FA8AD',
    usage: 'CTAs, links, focus rings, brand color',
  },
  {
    name: 'Primary strong',
    cssVar: '--color-corag-primary-strong',
    bgClass: 'bg-corag-primary-strong',
    textOnClass: 'text-white',
    lightHex: '#155054',
    darkHex: '#5BBFC4',
    usage: 'Hover, active, pressed states',
  },
  {
    name: 'Primary soft',
    cssVar: '--color-corag-primary-soft',
    bgClass: 'bg-corag-primary-soft',
    textOnClass: 'text-corag-text',
    lightHex: '#E1F2F1',
    darkHex: '#0F2A2C',
    usage: 'Tints, badges, subtle backgrounds',
  },
  {
    name: 'Accent',
    cssVar: '--color-corag-accent',
    bgClass: 'bg-corag-accent',
    textOnClass: 'text-corag-text',
    lightHex: '#E8A33D',
    darkHex: '#F4B95C',
    usage: 'Warm complement, large text only — NOT body text',
  },
  {
    name: 'Accent strong',
    cssVar: '--color-corag-accent-strong',
    bgClass: 'bg-corag-accent-strong',
    textOnClass: 'text-white',
    lightHex: '#B87C1F',
    darkHex: '#FFD27A',
    usage: 'Pressed accent, high-contrast accent text',
  },
];

const surfaceTokens: Token[] = [
  {
    name: 'Background',
    cssVar: '--color-corag-bg',
    bgClass: 'bg-corag-bg',
    textOnClass: 'text-corag-text',
    lightHex: '#FAFBFB',
    darkHex: '#08191A',
    usage: 'Page background — deep green-teal in dark mode',
  },
  {
    name: 'Bg elevated',
    cssVar: '--color-corag-bg-elevated',
    bgClass: 'bg-corag-bg-elevated',
    textOnClass: 'text-corag-text',
    lightHex: '#FFFFFF',
    darkHex: '#0F2A2C',
    usage: 'Cards, modals, elevated surfaces',
  },
  {
    name: 'Border',
    cssVar: '--color-corag-border',
    bgClass: 'bg-corag-border',
    textOnClass: 'text-corag-text',
    lightHex: '#E2E8E8',
    darkHex: '#1E3D40',
    usage: 'Dividers, card outlines',
  },
  {
    name: 'Border strong',
    cssVar: '--color-corag-border-strong',
    bgClass: 'bg-corag-border-strong',
    textOnClass: 'text-corag-text',
    lightHex: '#C9D4D5',
    darkHex: '#2A5256',
    usage: 'Emphasized dividers, focused outlines',
  },
];

const textTokens: Token[] = [
  {
    name: 'Text',
    cssVar: '--color-corag-text',
    bgClass: 'bg-corag-text',
    textOnClass: 'text-corag-bg',
    lightHex: '#0F2A2C',
    darkHex: '#E8F0EF',
    usage: 'Primary body, headings',
  },
  {
    name: 'Text secondary',
    cssVar: '--color-corag-text-secondary',
    bgClass: 'bg-corag-text-secondary',
    textOnClass: 'text-white',
    lightHex: '#4A6164',
    darkHex: '#B5C7C9',
    usage: 'Secondary body, descriptions',
  },
  {
    name: 'Text muted',
    cssVar: '--color-corag-text-muted',
    bgClass: 'bg-corag-text-muted',
    textOnClass: 'text-white',
    lightHex: '#6E8589',
    darkHex: '#8FA3A6',
    usage: 'Metadata, captions — large text only on bg',
  },
];

const stateTokens: Token[] = [
  {
    name: 'Success',
    cssVar: '--color-corag-success',
    bgClass: 'bg-corag-success',
    textOnClass: 'text-white',
    lightHex: '#2E8757',
    darkHex: '#4FB07F',
    usage: 'Confirmation, positive feedback',
  },
  {
    name: 'Warning',
    cssVar: '--color-corag-warning',
    bgClass: 'bg-corag-warning',
    textOnClass: 'text-white',
    lightHex: '#C68417',
    darkHex: '#E2A848',
    usage: 'Caution, pending state',
  },
  {
    name: 'Danger',
    cssVar: '--color-corag-danger',
    bgClass: 'bg-corag-danger',
    textOnClass: 'text-white',
    lightHex: '#B83A3A',
    darkHex: '#E0635F',
    usage: 'Errors, destructive actions',
  },
  {
    name: 'Info',
    cssVar: '--color-corag-info',
    bgClass: 'bg-corag-info',
    textOnClass: 'text-white',
    lightHex: '#1F6F73',
    darkHex: '#3FA8AD',
    usage: 'Informational (alias of primary)',
  },
];

const approvedPairings = [
  {
    context: 'Primary text on bg',
    classes: 'text-corag-text bg-corag-bg',
    ratio: 'Light ~16.5:1 · Dark ~14:1',
    status: 'AAA',
  },
  {
    context: 'Secondary text on bg',
    classes: 'text-corag-text-secondary bg-corag-bg',
    ratio: 'Light ~6.8:1 · Dark ~9:1',
    status: 'AA',
  },
  {
    context: 'Muted text on bg',
    classes: 'text-corag-text-muted bg-corag-bg',
    ratio: 'Light ~4.0:1 · Dark ~5.5:1',
    status: 'AA Large only',
  },
  {
    context: 'Primary on bg',
    classes: 'text-corag-primary bg-corag-bg',
    ratio: 'Light ~5.5:1 · Dark ~7.5:1',
    status: 'AA',
  },
  {
    context: 'White on primary',
    classes: 'text-corag-on-fill bg-corag-fill',
    ratio: 'Light ~5.0:1',
    status: 'AA',
  },
];

const forbiddenPairings = [
  {
    classes: 'text-corag-accent bg-corag-bg',
    reason:
      'Light ~2.4:1 — fails WCAG AA. Reserve accent for large text, icons, or pills with alternate background.',
  },
  {
    classes: 'text-gray-400 / text-gray-500',
    reason:
      'Fails WCAG AA on white. Use text-corag-text-secondary or text-gray-600 instead.',
  },
  {
    classes: 'dark:text-gray-400 / dark:text-gray-500',
    reason:
      'Fails WCAG AA on dark canvas. Use dark:text-corag-text-secondary or dark:text-gray-300 instead.',
  },
];

const allGroups = [
  { title: 'Brand', tokens: ptTokens },
  { title: 'Surfaces', tokens: surfaceTokens },
  { title: 'Text', tokens: textTokens },
  { title: 'State', tokens: stateTokens },
];
---

<ShowcaseLayout
  title="Colors"
  description="Corag design tokens — live swatches reflect the active light/dark mode."
  section="colors"
>
  <!-- Intro -->
  <section class="mb-10" data-component="corag-colors-intro">
    <div class="rounded-2xl border border-corag-border bg-corag-bg-elevated p-6">
      <p class="text-sm text-corag-text-secondary">
        Every public page uses the global Corag palette by default. Tokens are
        registered in <code class="text-xs">src/styles/global.css</code> via
        Tailwind 4 <code class="text-xs">@theme</code>; Tailwind generates the
        matching utility classes (<code class="text-xs">bg-corag-primary</code>,
        <code class="text-xs">text-corag-text</code>, <code class="text-xs">border-corag-border</code>, …).
      </p>
      <p class="mt-2 text-sm text-corag-text-secondary">
        Tokens are declared in one place only. Never override a
        <code class="text-xs">--color-corag-*</code> value outside
        <code class="text-xs">src/styles/global.css</code>.
      </p>
      <p class="mt-2 text-xs text-corag-text-muted">
        Source of truth: <a href="/internal/brand/colors" class="underline">Brand Book → Colors</a> ·
        <code class="text-xs">docs/BRAND_GUIDE.md</code> ·
        <code class="text-xs">docs/DESIGN.md</code>.
      </p>
    </div>
  </section>

  <!-- Token Groups -->
  {allGroups.map((group) => (
    <section class="mb-12" data-component={`tokens-${group.title.toLowerCase()}`}>
      <h2 class="text-xl md:text-2xl font-bold mb-4 text-corag-text">{group.title}</h2>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
        {group.tokens.map((t) => (
          <div class="rounded-2xl border border-corag-border overflow-hidden bg-corag-bg-elevated">
            <div class:list={['h-24 flex items-end p-3', t.bgClass, t.textOnClass]}>
              <code class="text-xs font-mono opacity-90">{t.bgClass}</code>
            </div>
            <div class="p-4">
              <div class="font-semibold text-corag-text">{t.name}</div>
              <code class="block text-xs text-corag-text-secondary mt-1">{t.cssVar}</code>
              <div class="grid grid-cols-2 gap-2 mt-2 text-xs">
                <div>
                  <span class="block text-corag-text-muted">Light</span>
                  <code class="text-corag-text">{t.lightHex}</code>
                </div>
                <div>
                  <span class="block text-corag-text-muted">Dark</span>
                  <code class="text-corag-text">{t.darkHex}</code>
                </div>
              </div>
              <p class="text-xs text-corag-text-secondary mt-3">{t.usage}</p>
            </div>
          </div>
        ))}
      </div>
    </section>
  ))}

  <!-- Live Light/Dark Comparison -->
  <section class="mb-12" data-component="light-dark-comparison">
    <h2 class="text-xl md:text-2xl font-bold mb-4 text-corag-text">Light vs Dark</h2>
    <p class="text-sm text-corag-text-secondary mb-4">
      Both modes side by side. The <strong>Live</strong> column reflects the
      mode you have active in the toggle above; the snapshot columns are
      always visible regardless.
    </p>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
      <div class="rounded-2xl overflow-hidden border border-corag-border" data-mode="light-snapshot" style="background-color:#FAFBFB;color:#0F2A2C;">
        <div class="px-4 py-2 border-b text-xs font-semibold uppercase tracking-wider" style="border-color:#E2E8E8;color:#4A6164;">Light snapshot</div>
        <div class="p-5">
          <div class="text-xl font-bold mb-2">Corag</div>
          <p class="text-sm" style="color:#4A6164;">El ecosistema de impacto social.</p>
          <button type="button" class="mt-4 inline-flex items-center px-4 py-2 rounded-md text-sm font-semibold text-white" style="background-color:#1F6F73;">Inscríbete al meetup</button>
        </div>
      </div>
      <div class="rounded-2xl overflow-hidden border border-corag-border bg-corag-bg" data-mode="live">
        <div class="px-4 py-2 border-b border-corag-border text-xs font-semibold uppercase tracking-wider text-corag-text-secondary">Live (matches your toggle)</div>
        <div class="p-5 text-corag-text">
          <div class="text-xl font-bold mb-2">Corag</div>
          <p class="text-sm text-corag-text-secondary">El ecosistema de impacto social.</p>
          <button type="button" class="mt-4 inline-flex items-center px-4 py-2 rounded-md text-sm font-semibold text-corag-on-fill bg-corag-fill hover:bg-corag-fill-strong transition-colors">Inscríbete al meetup</button>
        </div>
      </div>
      <div class="rounded-2xl overflow-hidden border" data-mode="dark-snapshot" style="background-color:#08191A;color:#E8F0EF;border-color:#1E3D40;">
        <div class="px-4 py-2 border-b text-xs font-semibold uppercase tracking-wider" style="border-color:#1E3D40;color:#B5C7C9;">Dark snapshot</div>
        <div class="p-5">
          <div class="text-xl font-bold mb-2">Corag</div>
          <p class="text-sm" style="color:#B5C7C9;">El ecosistema de impacto social.</p>
          <button type="button" class="mt-4 inline-flex items-center px-4 py-2 rounded-md text-sm font-semibold" style="background-color:#3FA8AD;color:#08191A;">Inscríbete al meetup</button>
        </div>
      </div>
    </div>
  </section>

  <!-- Approved Pairings -->
  <section class="mb-12" data-component="approved-pairings">
    <h2 class="text-xl md:text-2xl font-bold mb-4 text-corag-text">Approved text pairings (WCAG AA+)</h2>
    <div class="space-y-3">
      {approvedPairings.map((p) => (
        <div class="flex flex-col sm:flex-row sm:items-center gap-3 sm:gap-6 rounded-lg border border-corag-border p-4 bg-corag-bg-elevated">
          <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-bold uppercase bg-corag-success text-white shrink-0">{p.status}</span>
          <div class="flex-1">
            <div class="font-medium text-sm text-corag-text">{p.context}</div>
            <code class="block text-xs text-corag-text-secondary mt-1">{p.classes}</code>
          </div>
          <code class="text-xs text-corag-text-muted">{p.ratio}</code>
        </div>
      ))}
    </div>
  </section>

  <!-- Forbidden Pairings -->
  <section class="mb-12" data-component="forbidden-pairings">
    <h2 class="text-xl md:text-2xl font-bold mb-4 text-corag-text">Forbidden combinations</h2>
    <div class="space-y-3">
      {forbiddenPairings.map((p) => (
        <div class="flex flex-col sm:flex-row sm:items-start gap-3 sm:gap-6 rounded-lg border border-corag-border p-4 bg-corag-bg-elevated">
          <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-bold uppercase bg-corag-danger text-white shrink-0">FAIL</span>
          <div class="flex-1">
            <code class="text-xs font-medium text-corag-text">{p.classes}</code>
            <p class="text-xs text-corag-text-secondary mt-1">{p.reason}</p>
          </div>
        </div>
      ))}
    </div>
  </section>

  <!-- Usage example -->
  <section class="mb-12" data-component="usage-examples">
    <h2 class="text-xl md:text-2xl font-bold mb-4 text-corag-text">Usage</h2>
    <details class="rounded-lg border border-corag-border bg-corag-bg-elevated">
      <summary class="px-4 py-3 cursor-pointer text-sm font-medium text-corag-text hover:bg-corag-primary-soft">
        Show code
      </summary>
      <pre class="p-4 text-xs overflow-x-auto bg-corag-bg text-corag-text"><code>{`<!-- Page background + body text -->
<body class="bg-corag-bg text-corag-text">

<!-- Card on elevated surface -->
<div class="bg-corag-bg-elevated border border-corag-border rounded-lg p-4">
  <h3 class="text-corag-text">Title</h3>
  <p class="text-corag-text-secondary">Body copy</p>
</div>

<!-- Primary CTA -->
<button class="bg-corag-fill hover:bg-corag-fill-strong text-corag-on-fill px-4 py-2 rounded-md">
  Inscríbete
</button>

<!-- Accent badge (large text only) -->
<span class="bg-corag-primary-soft text-corag-primary px-2 py-0.5 rounded text-xs font-semibold">
  Speaker School
</span>

<!-- State -->
<div class="bg-corag-success text-white">Confirmed</div>
<div class="bg-corag-danger text-white">Cancelled</div>`}</code></pre>
    </details>
  </section>

  <!-- Agent Reference -->
  <section class="mt-16 border-t border-corag-border pt-8" data-agent-reference="true">
    <h2 class="text-lg font-bold mb-4 text-corag-text">Agent reference</h2>
    <p class="text-sm text-corag-text-secondary mb-4">
      Full machine-readable contract: <code class="text-xs">docs/DESIGN.md</code>.
      The Brand Book version of this page is the source of truth for design
      decisions: <a href="/internal/brand/colors" class="text-corag-primary underline">Brand Book → Colors</a>.
    </p>
    <div class="overflow-x-auto rounded-lg border border-corag-border">
      <table class="min-w-full text-sm">
        <thead class="bg-corag-primary-soft">
          <tr class="text-left text-corag-text">
            <th class="py-2 px-4 font-semibold">Token</th>
            <th class="py-2 px-4 font-semibold">CSS variable</th>
            <th class="py-2 px-4 font-semibold">Tailwind class</th>
            <th class="py-2 px-4 font-semibold">Light · Dark</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-corag-border">
          {allGroups.flatMap((g) => g.tokens).map((t) => (
            <tr>
              <td class="py-2 px-4 font-medium text-corag-text">{t.name}</td>
              <td class="py-2 px-4"><code class="text-xs text-corag-text-secondary">{t.cssVar}</code></td>
              <td class="py-2 px-4"><code class="text-xs text-corag-text-secondary">{t.bgClass}</code></td>
              <td class="py-2 px-4 text-xs text-corag-text-muted">{t.lightHex} · {t.darkHex}</td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  </section>
</ShowcaseLayout>
