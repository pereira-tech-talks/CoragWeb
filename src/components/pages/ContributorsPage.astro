---
/**
 * Corag v3.0.0 Equipo / Team directory — organizers, communities, companies.
 */

import AllyCard from '@/components/cards/AllyCard.astro';
import ContributorCard from '@/components/cards/ContributorCard.astro';
import Breadcrumbs from '@/components/ui/Breadcrumbs.astro';
import EmptyState from '@/components/ui/EmptyState.astro';
import Eyebrow from '@/components/ui/Eyebrow.astro';
import Section from '@/components/ui/Section.astro';
import MainLayout from '@/layouts/MainLayout.astro';
import { getAlliesByKind } from '@/lib/ally';
import { getCurrentTeamOrganizers } from '@/lib/contributor';
import { getUrlPrefix, type Language } from '@/lib/i18n';
import { getTranslations } from '@/lib/translations';

interface Props {
  lang: Language;
}

const { lang } = Astro.props;
const t = getTranslations(lang);
const cp = t.contributorsPage;
const prefix = getUrlPrefix(lang);

const currentOrganizers = await getCurrentTeamOrganizers();
const communities = await getAlliesByKind('community');
const companies = await getAlliesByKind('company');

const breadcrumbs = [
  { label: lang === 'es' ? 'Inicio' : 'Home', href: `${prefix}/` },
  { label: cp.title },
];
---

<MainLayout lang={lang} title={cp.title} description={cp.description}>
  <div class="relative overflow-hidden bg-corag-bg">
    <div
      class="pointer-events-none absolute inset-0 bg-[radial-gradient(ellipse_at_top,_var(--corag-primary)_0%,_transparent_55%)] opacity-[0.07] dark:opacity-[0.12]"
      aria-hidden="true"
    >
    </div>
    <div class="main-container relative pt-12 sm:pt-16 pb-4">
      <Breadcrumbs items={breadcrumbs} />
    </div>
    <div class="main-container relative pt-6 pb-14 sm:pb-16">
      <Eyebrow>{cp.eyebrow}</Eyebrow>
      <h1 class="mt-3 max-w-3xl text-4xl sm:text-5xl font-bold tracking-tight text-corag">
        {cp.title}
      </h1>
      <p class="mt-4 max-w-2xl text-lg text-corag-secondary">
        {cp.intro(currentOrganizers.length)}
      </p>
      <div class="mt-8 flex flex-wrap items-center gap-3">
        <a
          href={`${prefix}/contact/`}
          class="inline-flex min-h-[44px] items-center gap-2 rounded-full bg-corag-fill px-5 py-2.5 text-sm font-semibold text-corag-on-fill shadow-sm transition-colors hover:bg-corag-fill/90 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-corag-primary dark:bg-corag-primary-light dark:text-corag-bg-dark dark:hover:bg-corag-primary-light/90"
        >
          {cp.joinLabel}
        </a>
        <a
          href={`${prefix}/contributing/`}
          class="inline-flex min-h-[44px] items-center gap-2 rounded-full border border-corag-border bg-corag-bg-elevated px-5 py-2.5 text-sm font-semibold text-corag transition-colors hover:border-corag-primary/40 hover:bg-corag-primary-soft focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-corag-primary dark:border-white/15 dark:bg-corag-bg-dark dark:text-white dark:hover:bg-white/10"
        >
          {cp.contributeLabel}
        </a>
      </div>
    </div>
  </div>

  {
    currentOrganizers.length > 0 ? (
      <Section surface="alt" spacing="lg" title={cp.currentTitle}>
        <p class="mb-10 max-w-2xl text-corag-secondary">{cp.currentIntro}</p>
        <ul class="grid list-none gap-6 sm:grid-cols-2 lg:grid-cols-3">
          {currentOrganizers.map((contributor, index) => (
            <li>
              <ContributorCard
                contributor={contributor}
                lang={lang}
                loading={index < 6 ? 'eager' : 'lazy'}
              />
            </li>
          ))}
        </ul>
      </Section>
    ) : (
      <Section surface="alt">
        <EmptyState title={cp.emptyTitle} description={cp.emptyDesc} />
      </Section>
    )
  }

  {
    communities.length > 0 && (
      <Section spacing="lg" title={cp.communitiesTitle}>
        <p class="mb-10 max-w-2xl text-corag-secondary">{cp.communitiesIntro}</p>
        <ul class="grid list-none gap-6 sm:grid-cols-2 lg:grid-cols-3">
          {communities.map((ally, index) => (
            <li>
              <AllyCard
                ally={ally}
                lang={lang}
                loading={index < 4 ? 'eager' : 'lazy'}
              />
            </li>
          ))}
        </ul>
      </Section>
    )
  }

  {
    companies.length > 0 && (
      <Section surface="alt" spacing="lg" title={cp.companiesTitle}>
        <p class="mb-10 max-w-2xl text-corag-secondary">{cp.companiesIntro}</p>
        <ul class="grid list-none gap-6 sm:grid-cols-2 lg:grid-cols-3">
          {companies.map((ally, index) => (
            <li>
              <AllyCard
                ally={ally}
                lang={lang}
                loading={index < 4 ? 'eager' : 'lazy'}
              />
            </li>
          ))}
        </ul>
      </Section>
    )
  }
</MainLayout>
